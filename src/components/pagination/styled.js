import styled from "styled-components";

export const Box = styled.div`
  .buttons {
    width: 100vw;
    display: flex;
    justify-content: space-between;

    button {
      margin: 0 14vw 10px;
      padding: 2px;
      background: none;
      border: none;
    }
  }
`;
